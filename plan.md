1. **Understand the Drift Mechanism:** The `createAuditLog` function currently reads the last log's hash and appends a new log without verifying if the state has changed between the read and write operations. In a concurrent environment, two requests reading the same initial state could both generate a new log pointing to the same `previousHash`, leading to a race condition (read-modify-write drift) and diverging audit trails.
2. **Implement Optimistic Concurrency Control (OCC):** Modify the `createAuditLog` signature to require `expectedParentHash` as a mandatory parameter (before the optional `author` parameter).
3. **Enforce the Check:** Inside `createAuditLog`, check if `expectedParentHash` matches the actual `previousHash` of the latest log (or the root genesis hash if empty). If they do not match, throw an Error (e.g., "Concurrency drift detected").
4. **Test the Fix:** Ensure `src/utils/audit.test.ts` covers the concurrent simulation correctly: it should demonstrate that appending a second event using the initial parent hash will fail, while using the updated parent hash will succeed.
5. **Pre-commit checks & Submit:** Run linter, tests, and pre-commit checks.
